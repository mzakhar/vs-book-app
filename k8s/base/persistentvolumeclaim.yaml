apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vs-book-app-data
  namespace: vs-book-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
